import java.util.Scanner;

public class TalSpiral {

	public static void main( String[] args ){
		Scanner scan = new Scanner(System.in);
		int n = scan.nextInt();
		double previousSqrt = Math.floor(Math.sqrt(n));
		int perfectSquare = previousSqrt**2;
		System.out.println(previousSqrt);

	}
}